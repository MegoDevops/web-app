apiVersion: v1
kind: Service
metadata:
  name: garden-api
  labels:
    app: garden-api
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: garden-api