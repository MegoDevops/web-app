# === Terraform core directories ===
.terraform/
.terraform.lock.hcl

# === Terraform state files ===
terraform.tfstate
terraform.tfstate.backup
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# === Sensitive variable files ===
*.tfvars
*.tfvars.json

# === Local environment & temp files ===
*.backup
*.log
*.tmp

# === OS / Editor junk ===
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# === AWS specific local configs ===
.aws/
*.pem
*.key
*.crt

# === SSH Keys (private / public) ===
ssh-keys/
*.pem
*.key
*.pub

# === GitHub Actions & CI/CD temp ===
.github/workflows/*.tmp
.github/workflows/*.log
.github/workflows/.cache/
.github/workflows/.terraform/
.github/workflows/.aws/
*.lock
*.cache

# === Ansible inventory files ===
ansible/inventory.ini
ansible/inventory/
ansible/host_vars/
ansible/group_vars/
*.retry
# === Ansible vault files ===
*.vault
*.vault.yml
*.vault.yaml
# === Ansible temporary files ===
ansible/.vagrant/
ansible/.cache/
ansible/.ansible/
ansible/__pycache__/
ansible/*.log
ansible/*.tmp
